<Window x:Class="Lab_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_2"
        mc:Ignorable="d"
        Title="TextEditor" Height="450" Width="600">

    <Window.Resources>
        <LinearGradientBrush x:Key="LinearPanelBrush">
            <GradientStop Color="Yellow" Offset="0"></GradientStop>
            <GradientStop Color="Aqua" Offset="0.5"></GradientStop>
            <GradientStop Color="Yellow" Offset="0.8"></GradientStop>
        </LinearGradientBrush>

        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10">
            <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource LinearPanelBrush}">
                <Button Command="Open" Width="60" Height="60">
                    <Image ToolTip="Open" Source="images/Open.png"/>
                </Button>
                <Button Command="Cut" Width="60" Height="60">
                    <Image ToolTip="Clear" Source="images/Clear.png"/>
                </Button>
                <Button Command="Copy" Width="60" Height="60" CommandTarget="{Binding ElementName=inputTextBox}">
                    <Image ToolTip="Copy" Source="images/Copy.png"/>
                </Button>
                <Button Command="Paste" Width="60" Height="60" CommandTarget="{Binding ElementName=inputTextBox}">
                    <Image ToolTip="Paste" Source="images/Paste.png"/>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10">
            <Grid Background="{StaticResource LinearPanelBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <TextBox x:Name="inputTextBox" 
                         Grid.Row="0" 
                         Margin="10"
                         AcceptsReturn="True"
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Visible" 
                         FontSize="{Binding ElementName=fontSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"></TextBox>

                <DockPanel Grid.Row="1" Margin="10 5 10 10">
                    <Label>Розмір шрифту:</Label>
                    <Slider Margin="4" MinWidth="100" Name="fontSlider" Maximum="100" Minimum="12" VerticalAlignment="Bottom"></Slider>
                    <Button Width="30" DockPanel.Dock="Bottom" Style="{x:Null}" 
                            Content="OK" ToolTip="Save"
                            Command="Save"/>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</Window>